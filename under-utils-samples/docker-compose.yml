services:
  redis:
    image: redis:7-alpine
    container_name: under-utils-samples-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - under-utils-samples-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  under-utils-samples-redis-data:
